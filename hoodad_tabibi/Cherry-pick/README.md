`git cherry-pick` helps you apply the changes from a single commit onto your current branch.<br>
Run `git cherry-pick --help` for documentation.

### Start
```
git checkout <target_branch>
git cherry-pick <commit-hash>
```
